<%= simple_form_for(@teaching) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :teacher %>
    <%= f.association :classe %>
    <%= f.input :year %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
