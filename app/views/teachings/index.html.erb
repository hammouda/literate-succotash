<p id="notice"><%= notice %></p>

<h1>Teachings</h1>

<table class="table">

  <thead>
<tr><th colspan=2>
<%=form_tag(teachings_path, method: :get) do %>
<%= text_field_tag :term, params[:term] ,class:"search-input" %>
</th><th>
<%=submit_tag :Search  %>
<% end %>
</th>
<th colspan="3">
<%= link_to new_teaching_path do %>
   <%= image_tag "add.png", class: "add" %>
<% end %>
</th>
</tr>

    <tr>
      <th>Teacher</th>
      <th>Classe</th>
      <th>Year</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teachings.each do |teaching| %>
      <tr>
        <td><%= teaching.teacher %></td>
        <td><%= teaching.classe %></td>
        <td><%= teaching.year %></td>
        <td><%= link_to 'Show', teaching %></td>
        <td><%= link_to 'Edit', edit_teaching_path(teaching) %></td>
        <td><%= link_to 'Destroy', teaching, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

